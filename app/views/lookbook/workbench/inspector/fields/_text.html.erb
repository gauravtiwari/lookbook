<input
  type="text"
  id="param-<%= name %>"
  name="<%= name %>"
  value="<%= params[name] || default_value ||= "" %>"
  class="text-sm py-1.5 px-1.5 focus:ring-indigo-300 border-gray-300 focus:border-gray-200 w-full rounded"
  @keyup.stop.prevent="updateUrl"
>